import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.patrickcounty.gispro',
  appName: 'Patrick County GIS Pro',
  webDir: 'dist'
};

export default config;
